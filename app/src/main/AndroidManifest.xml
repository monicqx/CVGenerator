<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monic.cvgenerator">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".CreateCVActivity" />
        <activity android:name=".EducationActivity" />
        <activity android:name=".WorkExperienceActivity" />
        <activity android:name=".LanguagesActivity" />
        <activity android:name=".ITSkillsActivity" />
        <activity android:name=".OtherSkillsActivity" />
        <activity android:name=".CertificatesActivity" />
        <activity android:name=".ViewCVActivity" />
        <activity android:name=".TipsActivity" />
        <activity android:name=".ViewXMLActivity" />
        <activity android:name=".ProgressActivity"></activity>
    </application>

</manifest>